# Revit Server Activity Dashboard

Дашборд для мониторинга активности пользователей на Revit Server.

## Возможности

- 📊 **Статистика по пользователям**: количество синхронизаций и объем отправленных данных
- 📈 **График активности**: визуализация активности по дням для выбранной модели
- 📅 **Фильтрация по датам**: гибкая настройка временного диапазона
- 🔍 **Выбор сервера и модели**: детальный анализ конкретных проектов
- 📁 **Загрузка TSV/TXT файлов**: импорт данных из файлов

## Установка

### Предварительные требования

- Node.js (версия 16 или выше)
- npm или yarn

### Шаги установки

1. Распакуйте архив или клонируйте репозиторий

2. Установите зависимости:
```bash
npm install
```

или

```bash
yarn install
```

## Запуск

### Режим разработки

```bash
npm run dev
```

или

```bash
yarn dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

или

```bash
yarn build
```

### Предварительный просмотр продакшен-сборки

```bash
npm run preview
```

или

```bash
yarn preview
```

## Использование

1. **Загрузите файл с данными**
   - Нажмите кнопку "Выбрать файл"
   - Выберите TSV/TXT файл с данными Revit Server

2. **Настройте фильтр по датам**
   - Выберите дату начала и окончания периода
   - Система автоматически пересчитает статистику

3. **Просмотрите статистику по пользователям**
   - Таблица показывает всех уникальных пользователей
   - Отображается количество синхронизаций и объем данных в МБ

4. **Проанализируйте график активности**
   - Выберите сервер из выпадающего списка
   - Выберите конкретную модель
   - Изучите график активности по дням

## Формат данных

Приложение работает с TSV (Tab-Separated Values) файлами со следующими колонками:

- `Дата отчета` - дата формирования отчета
- `Сервер` - имя сервера
- `Путь к модели на Revit Server` - полный путь к файлу
- `Имя файла` - название файла модели
- `Date (UTC)` - дата и время синхронизации
- `ModelSize (байты)` - размер модели в байтах
- `SupportSize  (байты)` - размер поддерживающих файлов в байтах
- `User` - имя пользователя
- `VersionNumber` - номер версии

## Технологический стек

- **React 18** - библиотека для создания интерфейса
- **Vite** - быстрый сборщик и dev-сервер
- **Tailwind CSS** - utility-first CSS фреймворк
- **Recharts** - библиотека для визуализации данных
- **PapaParse** - парсинг CSV/TSV файлов

## Структура проекта

```
revit-server-dashboard/
├── src/
│   ├── components/          # React компоненты
│   │   ├── Header.jsx
│   │   ├── FileUpload.jsx
│   │   ├── DateFilter.jsx
│   │   ├── UserStatsTable.jsx
│   │   ├── ActivityChart.jsx
│   │   └── EmptyState.jsx
│   ├── utils/              # Утилиты
│   │   └── dataUtils.js
│   ├── App.jsx             # Главный компонент
│   ├── main.jsx            # Точка входа
│   └── index.css           # Глобальные стили
├── index.html
├── package.json
├── vite.config.js
├── tailwind.config.js
└── postcss.config.js
```

## Дальнейшие улучшения

- [ ] Интеграция с базой данных (MySQL/PostgreSQL)
- [ ] Экспорт отчетов в Excel/PDF
- [ ] Расширенная фильтрация и сортировка
- [ ] Сравнение периодов
- [ ] Уведомления о необычной активности

## Автор

Nikita - BIM специалист и разработчик

## Лицензия

MIT
